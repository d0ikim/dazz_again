# 크롤러가 필요한 Python 패키지 목록

# PostgreSQL 데이터베이스 연결용 라이브러리
psycopg2-binary==2.9.9

# .env 파일에서 환경변수를 읽기 위한 라이브러리
python-dotenv==1.0.0

# HTTP 요청을 보내는 라이브러리 (Kakao API 호출용)
requests==2.31.0

# HTML을 구조적으로 파싱하는 라이브러리 (클럽에반스 크롤러의 HTML 스크래핑용)
beautifulsoup4==4.12.3

# JSON 데이터를 다루기 위한 라이브러리
# (requests에 포함되어 있지만 명시적으로 추가)
